# android-ssh-proxy

Android-приложение (Android 10+) для системного HTTP проксирования трафика через SSH локальный порт‑форвардинг + VpnService.setHttpProxy.

## 📱 Как использовать

### 👤 Для обычного пользователя

**У вас уже есть готовый сервер с SSH доступом:**

1. **📲 Установите приложение** на Android 10+
2. **🔑 При первом запуске:**
   - Приложение создаст SSH ключ автоматически (зашифрованный с помощью Android Keystore)
   - Скопируйте публичный ключ из мастера настройки или раздела "Keys" 
3. **📋 Отправьте ключ администратору сервера** для добавления в `~/.ssh/authorized_keys`
4. **🌐 Добавьте сервер:**
   - Нажмите "+" в разделе "Servers"
   - Введите адрес сервера (IP или домен)
   - При необходимости измените порт SSH (по умолчанию 22)
   - Введите имя пользователя (обычно предоставляет администратор)
5. **🔌 Подключитесь:**
   - Выберите сервер и нажмите "Connect" 
   - Разрешите VPN доступ Android
   - Дождитесь статуса "Connected"
6. **✅ Готово!** Весь HTTP/HTTPS трафик теперь идет через сервер

### 🔧 Для системного администратора

**Настройка собственного прокси-сервера:**

1. **🖥️ Подготовьте Linux сервер** с SSH доступом
2. **📲 Установите приложение** и создайте SSH ключ
3. **📜 Скопируйте setup скрипт:**
   - Откройте раздел "Instructions" в приложении
   - Скопируйте и выполните автоматический скрипт на сервере
   - Или следуйте ручным инструкциям
4. **🛡️ Скрипт автоматически:**
   - Создаст ограниченного пользователя для SSH туннелирования
   - Установит HTTP прокси (Tinyproxy)
   - Настроит SSH ключ для входа
   - Ограничит доступ пользователя только до локального порт-форвардинга
   - Определит правильное имя SSH службы (ssh vs sshd) для разных OS
5. **⚙️ Дополнительные настройки:**
   - Настройте файрвол (порты 22, 8118)
   - Рассмотрите использование нестандартного SSH порта
   - Настройте логирование и мониторинг

**Структура на сервере:**
```bash
# Пользователь с ограниченными правами
/home/username/.ssh/authorized_keys  # Ваш публичный ключ

# HTTP прокси (Tinyproxy) слушает 127.0.0.1:8118
# SSH форвардинг: телефон:8080 -> сервер:8118
```

**Безопасность:**
- Пользователь имеет минимальные права (только port-forwarding)
- Прокси доступен только локально (127.0.0.1)
- SSH ключи зашифрованы с помощью Android Keystore (AES-256-GCM)
- Валидация host keys с предупреждениями о MITM атаках
- Поддерживается IPv6 для предотвращения обхода VPN

## 🔐 Функции безопасности

### Шифрование приватных ключей
- **Android Keystore**: Все приватные ключи зашифрованы аппаратно
- **AES-256-GCM**: Современное симметричное шифрование
- **TEE/Secure Element**: Ключи шифрования защищены на уровне железа
- **Zero-knowledge**: Даже root не может извлечь приватные ключи

### Валидация Host Keys (Known Hosts)
- **Автоматическое сохранение** при первом подключении
- **SHA-256 fingerprints** для идентификации серверов
- **Предупреждения MITM**: блокировка при изменении host key
- **Пользовательское подтверждение**: диалог для принятия изменений
- **Отображение отпечатков** серверов в интерфейсе

## Статус
⚠ Проект в активной разработке. Много недоработок, возможны крэши, утечки, несовместимости. Использовать только на свой риск.

## Что делает
- **Генерирует зашифрованные SSH ключи** (ECDSA по умолчанию, также RSA/Ed25519)
- **Валидирует host keys** серверов с предупреждениями о безопасности
- Показывает публичный ключ и готовые server setup инструкции (Tinyproxy, авто‑детект пакетного менеджера)
- Создает ограниченного пользователя на сервере (через предоставленный скрипт) для локального порт‑форвардинга
- Устанавливает SSH соединение и локальный форвард: 127.0.0.1:8080 -> 127.0.0.1:8118 (HTTP proxy на сервере)
- Поднимает VpnService без маршрутов, задает системный HTTP proxy = 127.0.0.1:8080 (Android Q+)
- **Автоматическое переподключение с exponential backoff стратегией**
- **Health-check мониторинг соединения через SSH keep-alive**
- **Ping мониторинг с отображением latency и качества соединения**
- **Умная обработка смены сетей** (Wi-Fi ↔ Mobile)
- **Сетевые тесты** для диагностики подключения
- Логи подключения с детальной диагностикой
- Управление серверами (host / user / port / name) с отображением fingerprints
- Инструкции + быстрый one‑liner скрипт развертывания
- **Мультиязычность** (English / Русский)
- **Темная тема** и системная тема
- GitHub Actions workflow: сборка и релиз по git tag (vX.Y.Z)

## Актуальные ограничения

| Тема | Деталь |
|------|--------|
| ✅ ~~Проверка host key~~ | **Реализована полная валидация** с Known Hosts и MITM защитой |
| ✅ ~~UI статус сервера~~ | **Реализован мониторинг** ping/latency с цветовыми индикаторами |
| Только HTTP proxy | Не модифицирует HTTPS / TLS напрямую, зависит от системной прокси поддержки приложений |
| Android < 10 | Не поддерживается (setHttpProxy недоступен) |
| Нет пер‑app исключений | Минималистичная конфигурация VPN без маршрутов, глобальный HTTP proxy |
| ✅ ~~Без устойчивости~~ | **Реализовано автоматическое переподключение** с exponential backoff и health-check |
| ✅ ~~Локальное хранение ключей~~ | **Приватные ключи зашифрованы** с помощью Android Keystore |
| Скрипт сервера | Выполняется с root / sudo, нужно вручную ревью перед запуском |

## Функции автоматического переподключения

### Health-check мониторинг
- Периодическая проверка состояния SSH соединения (по умолчанию каждые 30 секунд)
- Отправка SSH keep-alive пакетов для проверки доступности
- Автоматическое обнаружение разрыва соединения
- Мониторинг смены сетей (Wi-Fi ↔ Mobile Data)

### Exponential Backoff стратегия
- Начальная задержка: 1 секунда
- Максимальная задержка: 5 минут
- Множитель: 2x (удвоение задержки после каждой неудачной попытки)
- Максимум попыток: 10 (настраивается)

### Диагностические инструменты
- **Network Test**: проверка DNS, интернета, SSH сервера
- Детальные логи соединения
- Информация о активной сети и VPN статусе
- Отображение fingerprints серверов

### Настройки переподключения
В приложении доступны следующие настройки:
- **Внешний вид**: Выбор темы (системная, светлая, темная) и языка
- Включение/отключение автоматического переподключения
- Интервал health-check (секунды)
- Максимальное количество попыток переподключения
- Начальная задержка backoff (секунды)
- Максимальная задержка backoff (секунды)

## Мониторинг ping и качества соединения

### Реальное время отображение статуса
- **Ping**: Текущая latency до сервера в миллисекундах
- **Качество соединения**: Автоматическая оценка (Отличное/Хорошее/Удовлетворительное/Плохое)
- **Время соединения**: Длительность активного соединения
- **Цветовые индикаторы**: Визуальное отображение качества соединения

### Автоматический мониторинг
- Ping тесты каждые 10 секунд во время соединения
- История последних 10 ping результатов
- Автоматический расчет средней latency, min/max значений
- Отслеживание packet loss процента
- Цветовая индикация: 🟢 <50мс, 🟡 <200мс, 🟠 <500мс, 🔴 >500мс

## Умное кэширование External IP

### Адаптивные стратегии кэширования
- **Без VPN**: 10 минут кэш для обычного домашнего IP
- **С VPN**: Кэш на всю сессию VPN (до отключения)
- **Автоматическое переключение** стратегий при изменении VPN состояния

### Оптимизированные сетевые запросы  
- **Без запросов** при запуске приложения (показ кэша)
- **Обновление** только при VPN событиях (подключение/отключение)
- **Force refresh** через кнопку 🔄 для принудительного обновления
- **Множественные fallback** сервисы для надежного получения IPv4

### Предотвращение API блокировок
- Устранение HTTP 429 ошибок через редкие запросы  
- Валидация IPv4 формата (исключение IPv6)
- Разделение получения IP и определения страны
- Graceful fallback при недоступности geo-сервисов

## Архитектура (упрощенно)
- **SshProxyService**: SSH, локальный ServerSocket, LocalPortForwarder, VPN builder
- **ConnectionHealthMonitor**: Мониторинг состояния соединения и управление переподключением
- **PingMonitor**: Мониторинг latency и качества соединения с сервером
- **SecureHostKeyVerifier**: Валидация SSH host keys с защитой от MITM
- **KnownHostsManager**: Управление известными хостами (аналог ~/.ssh/known_hosts)
- **KeystoreManager**: Шифрование/расшифровка приватных ключей
- **SecurityNotificationManager**: Критические уведомления безопасности
- **UI**: Fragments (Home с ping статистикой, Servers, Keys, Instructions, Log, Setup flow, Settings)
- **Data**: Repositories (ServerRepository, KeyRepository, PreferencesManager), ConnectionStatus
- **Логи**: AppLog (LiveData с детальной диагностикой)
- **Мастер настройки**: Пошаговая настройка для новых пользователей

## Сборка (локально)
```bash
./gradlew assembleDebug
# или
./gradlew assembleRelease
```

## Улучшения в последних версиях

### ✅ v0.1.8 - Мониторинг соединения и умное кэширование
- **Ping мониторинг в реальном времени**: Отображение latency и качества соединения
- **Визуальные индикаторы качества**: Цветовое кодирование ping результатов  
- **Статистика соединения**: Время подключения, средняя latency, packet loss
- **Автоматические ping тесты**: Мониторинг каждые 10 секунд во время соединения
- **Улучшенный UI статуса**: Расширенная информация о состоянии соединения
- **Умное IP кэширование**: 10 минут без VPN, весь сеанс с VPN
- **Надежное IPv4 определение**: Множественные fallback сервисы
- **Оптимизированные сетевые запросы**: Только при VPN событиях или ручном обновлении

### ✅ v0.1.7 - Безопасность
- **Android Keystore шифрование**: Все приватные ключи зашифрованы аппаратно
- **Валидация Host Keys**: Полная реализация Known Hosts с MITM защитой
- **Отображение fingerprints**: Видимые отпечатки серверов в интерфейсе
- **Критические уведомления**: Предупреждения о изменениях host key
- **Упрощенная установка**: Убран импорт ключей, только безопасная генерация

### ✅ v0.1.5.1 - Стабильность скриптов
- **Автоопределение SSH службы**: Правильная работа с Ubuntu (ssh vs sshd)
- **Исправления bash переменных**: Корректная подстановка в скриптах
- **Только Tinyproxy**: Упрощенная настройка прокси сервера

### ✅ v0.1.5 - Пользовательский опыт  
- **Мастер настройки**: Пошаговая настройка для новых пользователей
- **Темная тема**: Поддержка системной, светлой и темной тем
- **Мультиязычность**: Английский и русский языки
- **Network Testing**: Встроенные тесты сети и подключения

## Возможные улучшения (TODO)
- [ ] Экспорт / импорт конфигурации
- [ ] Пер‑app proxy исключения (через addDisallowedApplication)
- [ ] Статистика трафика и использования
- [ ] 2FA поддержка для SSH

## Отладка
```bash
# Основные компоненты
adb logcat -s SshProxyService AppLog ConnectionHealthMonitor

# Безопасность
adb logcat -s SecureHostKeyVerifier KnownHostsManager KeystoreManager

# Мониторинг ping и IP
adb logcat -s PingMonitor IpLocationService

# Все компоненты приложения
adb logcat | grep -E "(SshProxy|SSH|VPN|Ping|android-ssh-proxy)"
```

## Отказ от ответственности
ПО предоставляется "как есть", без гарантий. Использование может нарушать политики сетей / провайдеров. Приложение реализует криптографические функции - убедитесь в соответствии локальным законам.

## Лицензия
Apache-2.0

---
**Проект активно развивается.** Сообщайте об issues и присылайте PR с улучшениями.